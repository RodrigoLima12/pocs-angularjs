import { start } from 'repl';
import angular = require('angular');

export class GoogleAnalyticsController {

    static $inject = ['$window', '$rootScope', '$location', '$transitions'];

    constructor($window: any, $rootScope: any, $location: any, $transitions: any) {

		// para implementação
    }
}
